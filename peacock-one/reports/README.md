# reports/

Output directory for generated Peacock SEO + GEO reports (e.g. exported JSON
from `engines/geo/site_intelligence`). Nothing is written here automatically
yet — this folder is reserved so a future export feature has a stable,
version-control-friendly home outside the application code.

Generated report files should not be committed; see `.gitignore`.
