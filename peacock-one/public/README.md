# public/

Repository-wide static assets that are not specific to the Next.js frontend
build (e.g. shared brand assets referenced from docs or reports).

The frontend's own static assets served by Next.js live in
[`frontend/public/`](../frontend/public/), since Next.js requires that folder
to sit alongside its own `src/app` directory.
